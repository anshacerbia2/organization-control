name: Architecture Governance

on:
  pull_request:
  push:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  architecture:
    uses: anshacerbia2/scnehaux-architecture/.github/workflows/linter.yml@main
    with:
      target_dir: docs
